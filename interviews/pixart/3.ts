const solution = (s_json) => {
  if (!s_json) {
    return false
  }

  let i = 0
  
  const skipEmpty = () => {
    while (!s_json[i]) {
      i++
    }
  }

  while (i < s_json.length) {
    const char = s_json[i]
  }

  return
}
